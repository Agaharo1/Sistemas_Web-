<% function mostrarChats(chats) { %>
    <div class="chat-list">
        <% if (chats && chats.length > 0) { %>
            <ul>
                <% chats.forEach(chat => { %>
                    <li class="chat-item">
                        <a href="/chats/chat/<%= chat.chatId %>">
                            <div class="chat-info"></div>
                                <h4><%= chat.nombre %></h4>
                                <p><%= chat.lastMensaje %></p>
                            </div>
                            
                        </a>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No tienes chats disponibles.</p>
        <% } %>
    </div>
<% } %>

<% mostrarChats(chats); %>